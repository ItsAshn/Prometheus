# Admin Configuration
# Change these values for your deployment
ADMIN_USERNAME=admin
ADMIN_PASSWORD=test

# JWT Secret - Will auto-generate if not set or insecure
# Run 'npm run setup' for easy configuration
JWT_SECRET=auto-generated-on-startup-if-needed

# Docker Configuration
# These are used by docker-compose and update scripts
IMAGE_NAME=itsashn/prometheus
IMAGE_TAG=latest
PORT=3000

# Note: APP_VERSION, BUILD_DATE, and VCS_REF are automatically
# included in the Docker image when it's built on Docker Hub.
# Don't set them here as they will override the image's built-in values.

# Environment
NODE_ENV=production